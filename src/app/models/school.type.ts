export interface SummerSchool {
    name: string,
    year: number,
    topics: string[],
    webpage: string
}

export interface School {
    dateFrom: string,
    dateTo: string,
    name: string,
    result: string,
    scholarships: string[],
    note: string
}
export interface SummerSchool {
    name: string,
    year: number,
    topics: string[],
    webpage: string
}

export interface School {
    dateFrom: Date,
    dateTo: Date,
    name: string,
    result: string,
    scholarships: string[],
    note: string
}